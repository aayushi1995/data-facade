import { Box, BoxProps } from "@mui/material";
import { styled } from "@mui/styles";


export const ExecuteActionMainBox = styled(Box)<BoxProps>(({theme}) => ({
    background: "#EBF1FA",
    boxShadow: "0px 1px 1px rgba(0, 0, 0, 0.1), 0px 0px 1px rgba(0, 0, 0, 0.25)",
    borderRadius: "8px"
}))